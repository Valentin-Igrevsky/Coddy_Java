# Контрольная работа №1 (18.01.26)

```java
import java.util.Scanner;

public class Main {
    static Scanner scanner = new Scanner(System.in);

    public static void main(String[] args) {
        System.out.println("===== Задание 1. Переменные и ввод данных через консоль =====");
        task_1();

        System.out.println("\n===== Задание 2. if-else =====");
        int[] scores = {95, 85, 75, 65, 55, 45, 35, 25, 15, 5};
        for (int i = 0; i < scores.length; i++) {
            System.out.print("Значение: " + scores[i] + "; Оценка: ");
            task_2(scores[i]);
        }

        System.out.println("\n===== Задание 3. switch-case =====");
        for (int i = 0; i < 6; i++) {
            System.out.print("Значение: " + i + "; День недели: ");
            task_3(i);
        }

        System.out.println("\n===== Задания 4 и 5. Одномерный массив и цикл for =====");
        task_45();

        System.out.println("\n===== Задания 6 и 7. Двумерный массив и вложенные циклы for =====");
        task_67();

        System.out.println("\n===== Задание 8. Цикл while и операторы continue и break =====");
        task_8();

        System.out.println("\n===== Задания 9 и 10. Классы и интерфейсы =====");
        task_910();

        System.out.println("\n===== Задание 11. Enum =====");
        for (TaskPriority priority : TaskPriority.values()) {
            System.out.print(priority + " ");
        }
    }

    // ===== Задание 1. Переменные и ввод данных через консоль =====
    static void task_1() {
        System.out.print("Введите название задачи: ");

        // Пункт 1
        // Создайте строковую переменную taskName и запишите в нее с помощью scanner название задачи через консоль
        // Подсказка: чтобы записать строку через консоль нужно использовать scanner.nextLine();
        // ...

        // Пункт 2
        // Создайте целочисленную переменную duration, дробную переменную progress, булеву переменную isCompleted.
        // Запишите любые значения (для duration и progress отличные от нуля) в созданные переменные
        // ...

        System.out.println("Название задачи: " + taskName + "\nПродолжительность: " + duration + " мин\nПрогресс: " + progress + "%\nЗавершена: " + isCompleted);
    }

    // ===== Задание 2. if-else =====
    static void task_2(int score) {
        // Напишите конструкцию if - else if - else:
        // Если значение переменной score больше или равно 90, вывести в консоль: "Отлично"
        // Иначе если значение переменной score больше или равно 70 и меньше 90, вывести в консоль: "Хорошо"
        // Иначе если значение переменной score больше или равно 50 и меньше 70, вывести в консоль: "Удовлетворительно"
        // Иначе вывести: "Неудовлетворительно"
        // Подсказка: Вывод в консоль осуществляется через System.out.println("Сообщение")
        // ...
    }

    // ===== Задание 3. switch-case =====
    static void task_3(int dayNumber) {
        // Напишите конструкцию switch-case, где проверяется значение переменной dayNumber:
        // Проверяемые значения: 1, 2, 3, 4, 5
        // При срабатывании одного из случаев должно вывестись уникальное сообщение в консоль (например, название дня недели)
        // Если значение переменной dayNumber не соответствует 1, 2, 3, 4, 5, должно вывестить сообщение по умолчанию (default)
        // Подсказка: не забывайте про оператор break
        // ...
    }

    // ===== Задания 4 и 5. Одномерный массив и цикл for =====
    static void task_45() {
        // Задание 4
        // Создайте одномерный массив temperatures для 6 целых чисел (int)
        // ...

        temperatures[0] = 25;
        temperatures[1] = -5;
        temperatures[2] = 18;
        temperatures[3] = 0;
        temperatures[4] = 32;
        temperatures[5] = -10;

        // Задание 5
        // Напишите цикл for, который "пройдется" по всем элементам массива temperatures и выведет их в консоль
        // Если какой-то элемент массива будет меньше нуля необходимо пропустить его и не выводить в консоль
        // Подсказка: необходимо использовать оператор continue
        // ...
    }

    // ===== Задания 6 и 7. Двумерный массив и вложенные циклы for =====
    static void task_67() {
        // Задание 6
        // Создайте двумерный массив целочисленных значений размером 4x4 (т.е. 4 массива по 4 элемента в каждом) и заполните значения, используя фигурные скобки { }
        // ...

        // Задание 7
        // С помощью вложенных циклов for выведите все элементы массива
        // Подсказка: вложенные циклы - это один цикл внутри другого
        // ...
    }

    // ===== Задание 8. Цикл while и операторы continue и break =====
    static void task_8() {
        int[] numbers = {12, -3, 7, 0, 25, -8, 14, 9, -1, 5};
        int product = 1;
        int i = 0;

        // Используя цикл while посчитайте произведение положительных четных чисел в массиве numbers
        while (true) {
            // Если число отрицательное или нечетное нужно перейти к следующей итерации цикла
            // ...

            // Если число равно нулю завершить работу цикла
            // ...

            product *= numbers[i];
            i += 1;
        }

        System.out.println("Произведение положительных четных чисел: " + product);
    }

    // ===== Задания 9 и 10. Классы и интерфейсы =====
    static void task_910() {
        // Задание 9
        // Пункт 1
        /* За пределами класса Main необходимо написать класс Task.
        У задачи должно быть:
            1) Публичное поле названия (title)
            2) Приватное поле текущего прогресса (progress)
            3) Приватное и финальное поле максимально возможного прогресса (MAX_PROGRESS)
            4) Конструктор по умолчанию, который принимает название и текущее значение прогресса
                4.1) Если текущее значение прогресса передано больше чем MAX_PROGRESS - записать в текущий прогресс MAX_PROGRESS
                4.2) Если текущее значение прогресса передано меньше нуля - записать 0
            5) Публичный метод, который возвращает текущее значение прогресса
            6) Публичный метод, который устанавливает прогресс в MAX_PROGRESS
        */

        // Пункт 1
        // Здесь создайте экземпляр класса Task
        // ..

        // Здесь вызовите все возможные методы у созданного экземпляра класса
        // ...

        // Задание 10
        /* По написанному классу напишите интерфейс TaskItem вне класса Main */
    }
}

// ===== Задание 11. Enum =====
/* Напишите перечисление (enum) TaskPriority
Перечисление должно иметь 4 значения: LOW, MEDIUM, HIGH, URGENT */
// ...

// Класс Task тут
// ...

// Интерфейс TaskItem тут
// ...
```